import {notFound} from 'next/navigation';
export default async function PreviewQA({searchParams}:{searchParams:Promise<Record<string,string>>}){if(process.env.NODE_ENV!=='development')notFound();const q=await searchParams;const width=['320','390','768','1024'].includes(q.width)?q.width:'390';const path=/^\/(en|it|lv|ru)\/[a-z0-9\/-]*$/.test(q.path||'')?q.path:'/en/';return <div style={{background:'#e1dddd',minHeight:'100vh',padding:25}}><h1 style={{fontFamily:'Arial',fontSize:18,marginBottom:20}}>BM HAIR · responsive QA · {width}px</h1><iframe title="BM HAIR responsive preview" src={path} width={width} height="820" style={{background:'white',border:'1px solid #999',display:'block',margin:'auto',maxWidth:'100%'}}/></div>;}
